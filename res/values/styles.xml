<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="TdPrintingTheme" parent="android:style/Theme.Holo.Light">
        <!-- Set background color here will also affect action bar -->
        <!-- <item name="android:background">@color/default_activity_bg</item> -->

        <!-- <item name="android:textColorPrimaryInverse">@android:color/primary_text_light</item> -->

        <item name="android:actionBarStyle">@style/TdPrintingTheme.ActionBar</item>
    </style>

    <style name="TdPrintingTheme.ActionBar" parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/default_action_bar_bg</item>
        <!-- <item name="android:backgroundSplit">@color/default_action_bar_bg</item> -->

        <item name="android:titleTextStyle">@style/TdPrintingTheme.ActionBar.TitleText</item>
    </style>

    <style name="TdPrintingTheme.ActionBar.TitleText"
            parent="android:style/TextAppearance.Holo.Widget.ActionBar.Title">
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
